server.port=8080
spring.application.name=users-service

# MongoDB Configuration - CORREGIDO
spring.data.mongodb.uri=mongodb+srv://manuelguarnizog_db_user:slmEouXGl5LaTDbg@clustereciexpress.m7ljcsf.mongodb.net/users-management-db?retryWrites=true&w=majority&appName=ClusterECIEXPRESS

# Swagger/OpenAPI Configuration
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# Logging
logging.level.com.tucompany.users=DEBUG
logging.level.org.springframework.data.mongodb=DEBUG
logging.level.org.mongodb.driver=INFO

# Security
# spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080

# Redis Connection - COMENTADO para Azure
# spring.data.redis.host=localhost
# spring.data.redis.port=6379
# spring.data.redis.database=0
# spring.data.redis.timeout=2000ms
# spring.data.redis.connect-timeout=2000ms

# Redis Listener - DESACTIVADO para Azure
redis.listener.enabled=false

# Configuración para recuperación de contraseña
app.password-reset.code-expiry-minutes=15
app.password-reset.max-attempts=3

# Para serialización de Value Objects en Redis
spring.jackson.visibility.property-access=PUBLIC_ONLY
spring.jackson.default-property-inclusion=NON_NULL

# Management - CONFIGURACIÓN MEJORADA
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
management.health.mongo.enabled=false
management.health.redis.enabled=false

# Desactivar autoconfiguración de Redis
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.data.redis.RedisAutoConfiguration